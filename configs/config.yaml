# MoviePilot-Go 配置文件

# 项目配置
project_name: "MoviePilot-Go"
version: "1.0.0"

# 服务器配置
host: "0.0.0.0"
port: 3001

# 数据库配置
db_type: "sqlite"
db_path: "./data/moviepilot.db"

# 日志配置
log_level: "info"
log_path: "./logs"

# 媒体配置
media_paths:
  - "./media"

# 插件配置
plugin_path: "./plugins"